# 600B Timelock TCG — Session Handover (2026-07-30)

Paste this into a fresh agent session (Claude Code or Codex) working in `G:\Github\TCG600nap`.

## State

- GitHub: **BIMbeamFLX/TCG600nap** (private). **PR #1 open:** `fix/e1-final-art-qa` → `main`
  (complete Edition One). `main` holds only the pipeline scaffold until the PR merges.
- 54/54 pytest green, ruff clean, working tree clean at handover.
- Repo is ~470 MB (295 card faces + 295 illustrations at 1920×2400). GitHub rejects the
  whole history as one HTTPS pack — push commit-by-commit
  (`git rev-list --reverse main..BRANCH` loop) or move `art/` to Git LFS later.

## What exists

- `cards/e1-cards.json` — canonical 295-card E1 text lock (+ `promos.json`);
  `cards/e1-art-prompts.json` — prompts-v2, one unique deterministic art prompt per card.
- `art/cards/final/<Name>.jpg` faces · `art/illustrations/E1-XXX.jpg` standalone art ·
  `art/world-plates/` six affinity environments.
- `site/` (English, static, relative paths, nsite-ready):
  - `index.html` landing — NIP-07 sign-in (vanilla bech32 npub encoder, kind-0 profile
    via damus/nos.lol/primal), zap-duel honor-system section
  - `cards.html` gallery — grid shows the full card, hover flips to artwork, click opens
    popup with artwork + rules + Simple Guide. **Generated by `scripts/build_gallery.py`
    — edit the generator, never the HTML.**
  - `arena.html` board mockup (world-plate skins, rulebook zones) · `rules.html` rulebook
- `docs/`: `napplet-spec.md` (P2P architecture), `rules-audit-e1.md`,
  `text-consistency-e1.md` (incl. slop metrics), `design-review-cards.md`,
  `codex-kickoff-e1-fixes.md` (ready-to-paste fix-pass prompt).
- Cockatrice: `cards/cards.csv` → `scripts/build_set.py --install`
  (affinity→color adapter: S=W, T=U, K=B, P=R, B=G).
- Official napplet agent skills installed at `G:\Github\.claude\skills\`
  (make/design/build/test/port-napplet).

## Conventions & gotchas

- Voice: positive cypherpunk, **educational AND funny**. Never investment language
  ("bullish"), never "Web5", no MTG vocabulary (rulebook §23 map).
- **Single-writer rule:** Claude and Codex share this working tree — check `git status`
  before touching shared files. Branches + PR to main, Conventional Commits,
  ruff + pytest before every commit.
- Python: **always `.venv/Scripts/python`** (the PATH `python` is an unrelated hermes
  3.11 venv). Codex sometimes strips the venv — repair with
  `.venv/Scripts/python -m ensurepip` then `pip install ruff pytest pillow`.
- Dev server: launch config `600b-site` → http://localhost:8600 (serves the repo root).
- Audit-first pipeline: generators record to `.audit/*.sqlite` before writing public
  artifacts; source/reference data stays read-only.

## Open tasks, in order

1. Review + merge **PR #1**.
2. Run the fix pass: paste `docs/codex-kickoff-e1-fixes.md` into a fresh session.
   It covers: legacy terms on 14 cards (Wall→Firewall, regenerated→Rebooted, haste→Boot
   Delay, nonblack→non-Keys), canonical keyword reminders, Broadcast Guard §14 adoption,
   protocol-note dedup (≤2× per fact), flavor de-slop (catchphrase rotation out),
   design D1–D4 in the face renderer, `*/*` stats rulebook paragraph. Re-render only
   affected faces, rebuild gallery + Cockatrice adapter.
3. Optional next: flip the repo public · deploy `site/` as nsite (`/nsite-deploy`) ·
   `/build-napplet` for the playable P2P version.

## Decisions already made (do not relitigate)

- Moves are **pure WebRTC**, no move locks; Nostr events only for invites and match
  results/stats. Hidden information via commit-reveal (hash of decklist+seed at start,
  reveal at end). `webrtc.open({scope:{type:'direct',pubkey}})` — the shell brokers
  signaling.
- Zap wagers v1 = **honor system** (the app never holds funds; it opens the NIP-57 zap
  to the winner). Cashu escrow deferred: no value-NAP yet, gambling-law caution, and
  rulebook §19.1 currently forbids value transfer → future opt-in module + legal review.
- Broadcast Guard: adopt into §14 (default). E1-191 rename default:
  "Mtoshi, Lethal Courier".
