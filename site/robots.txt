# 600B Timelock TCG — a positive cypherpunk card game. MIT licensed.
# Everything a player would want to read is open; the two disallowed paths are
# both workshop documents rather than pages written for a reader.
#
# fx-demo.html is a developer harness for the table's effects.
#
# e1-card-set.html is no longer the 18-card playtest slice this file used to
# say it was: build_card_set.py now lays out every shipped face — all 296,
# plus the shared back — with trim guides, for proofing against the press. It
# stays closed because it is the same catalog cards.html already serves, in a
# form meant for a printer: 300 kB of markup, no search, no rules notes, and
# nothing a player is looking for. cards.html is the page that should be found.

User-agent: *
Allow: /
Disallow: /fx-demo.html
Disallow: /e1-card-set.html

# The referee's socket and JSON API are not documents.
Disallow: /ws
Disallow: /api/

# No Sitemap line on purpose: a sitemap must carry absolute URLs, and no
# canonical domain is fixed yet. Add it at publish time, when the answer is
# known, rather than shipping one pointing at a guess.
